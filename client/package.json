{
  "name": "brainless-frontend-client",
  "version": "0.1.0",
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-quill": "^2.0.0-beta.4",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "postbuild": "react-snap && rimraf build/200.html build/asset-manifest.json build/**/*.map build/**/main.*.js build/**/*.chunk.js build/**/*.js.LICENSE.txt"
  },
  "reactSnap": {
    "removeScriptTags": true
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-snap": "https://github.com/SW999/react-snap.git",
    "rimraf": "^3.0.2",
    "http-proxy-middleware": "2.0.6"
  }
}
